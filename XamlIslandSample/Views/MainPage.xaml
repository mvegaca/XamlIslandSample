<Page
    x:Class="XamlIslandSample.Views.MainPage"
    Style="{DynamicResource MahApps.Styles.Page}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="clr-namespace:XamlIslandSample.Controls"
    xmlns:properties="clr-namespace:XamlIslandSample.Properties"    
    mc:Ignorable="d"
    d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock
            Style="{StaticResource PageTitleStyle}"
            Margin="{StaticResource MediumLeftMargin}"
            Text="{x:Static properties:Resources.MainPageTitle}" />
        <StackPanel Grid.Row="1" Margin="{StaticResource MediumLeftRightMargin}">
            <controls:NewCustomControl
                Text="{Binding Text, Mode=TwoWay}"
                AppColors="{Binding AppColors, Mode=OneWay}" />
            <TextBlock Margin="{StaticResource SmallTopMargin}" Text="WPF ViewModel Property" />
            <TextBox Margin="{StaticResource SmallTopMargin}" Text="{Binding Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>
    </Grid>
</Page>
